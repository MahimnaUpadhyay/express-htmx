<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <script src="https://unpkg.com/htmx.org@2.0.3"
        integrity="sha384-0895/pl2MU10Hqc6jd4RvrthNlDiE9U1tWmX7WRESftEDRosgxNsQG/Ze9YMRzHq"
        crossorigin="anonymous"></script>
    <title>Express + HTMX</title>
</head>

<body>
    <div class="container p-5">
        <div class="mx-auto p-5 shadow-lg rounded bg-light" style="max-width: 500px;">
            <h2 class="text-center mb-4 text-primary">Todo List</h2>
            <form class="mx-auto d-flex align-items-center mb-4" hx-post="/post-todo" hx-target="#todo-list-container"
                hx-swap="beforeend">
                <input type="text" name="task" placeholder="Enter a new task" class="form-control me-2" required />
                <button type="submit" class="btn btn-primary">
                    Add
                </button>
            </form>

            <div>
                <ol class="list-group" id="todo-list-container">
                    <% todo.forEach(function(data) { %>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <div class="col justify-content-between align-items-center">
                                <span class="<%= data.isCompleted ? 'text-decoration-line-through' : '' %>">
                                    <%=data.id %> <%= data.task %>
                                </span>
                                <% if (data.isCompleted) { %>
                                    <span class="badge bg-success rounded-pill ms-2">Completed</span>
                                    <% } else { %>
                                        <span class="badge bg-warning rounded-pill ms-2">Pending</span>
                                        <% } %>
                            </div>
                        </li>
                    <% }); %>
                </ol>
            </div>
        </div>
    </div>
</body>

</html>